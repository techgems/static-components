@page
@model StaticComponents.Web.Pages.StaticScriptTests.WithSrcModel
@{
    ViewData["Title"] = "static-script with src";
}

<h1>static-script — with <code>src</code></h1>

<p>A <code>&lt;script static-script&gt;</code> that only has a <code>src</code> attribute renders without throwing.</p>

<static-script-with-src-component></static-script-with-src-component>
