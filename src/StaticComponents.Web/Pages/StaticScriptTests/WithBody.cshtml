@page
@model StaticComponents.Web.Pages.StaticScriptTests.WithBodyModel
@{
    ViewData["Title"] = "static-script with body";
}

<h1>static-script — with body</h1>

<p>A <code>&lt;script static-script&gt;</code> with an inline body renders the script inline. Open the browser console to see its output.</p>

<static-script-with-body-component></static-script-with-body-component>
