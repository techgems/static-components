@using StaticComponents.Web.Views.StaticScriptTests
@model StaticScriptWithSrcComponent

<div>
    <p>This component renders a <code>static-script</code> that only has a <code>src</code> attribute (no inline body).</p>

    <script static-script type="text/javascript" src="/js/static-script-src-test.js"></script>
</div>
